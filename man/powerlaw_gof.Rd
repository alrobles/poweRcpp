% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poweRcpp.R
\name{powerlaw_gof}
\alias{powerlaw_gof}
\title{Goodness-of-fit test for a discrete power-law distribution}
\usage{
powerlaw_gof(data, alpha = NULL, x_min = NULL, replicas = 1000L,
  type = c("left", "right"))
}
\arguments{
\item{data}{Positive integer vector of observations.}

\item{alpha}{Power-law exponent.  When \code{NULL} (default) it is
estimated from the data.}

\item{x_min}{Lower cut-off.  When \code{NULL} (default) it is estimated
from the data.}

\item{replicas}{Number of bootstrap replicas (default \code{1000}).
Values \eqn{\geq 1000} are recommended for stable p-values.}

\item{type}{Distribution type (\code{"left"} or \code{"right"}).}
}
\value{
A named list with elements:
\describe{
  \item{p_value}{Bootstrap p-value.}
  \item{ks_statistic}{Observed KS statistic.}
  \item{alpha}{Alpha used for the test.}
  \item{x_min}{xMin used for the test.}
  \item{replicas}{Number of replicas used.}
}
}
\description{
Computes a p-value for the hypothesis that \code{data} are drawn from a
discrete power-law distribution.  A semi-parametric bootstrap procedure
(Clauset, Shalizi & Newman 2009) is used.  The bootstrap loop is
parallelised with \pkg{RcppParallel}.
}
\examples{
\dontrun{
set.seed(42)
data <- sample(1:500, 200, replace = TRUE, prob = (1:500)^(-2.5))
gof  <- powerlaw_gof(data, replicas = 500)
cat("p-value:", gof$p_value, "\n")
}
}
\references{
Clauset, A., Shalizi, C.R. & Newman, M.E.J. (2009).
Power-law distributions in empirical data.
\emph{SIAM Review}, 51(4), 661-703.
\doi{10.1137/070710111}
}
