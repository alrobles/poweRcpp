% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{powerlaw_gof_cpp}
\alias{powerlaw_gof_cpp}
\title{Goodness-of-fit test for a discrete power-law distribution}
\usage{
powerlaw_gof_cpp(
  data,
  alpha = NULL,
  x_min = NULL,
  replicas = 1000L,
  type = "left"
)
}
\arguments{
\item{data}{Integer vector of observations.}

\item{alpha}{Power-law exponent.  If \code{NA} (default) it is estimated
from \code{data}.}

\item{x_min}{Lower cut-off.  If \code{NA} (default) it is estimated
from \code{data}.}

\item{replicas}{Number of bootstrap replicas (default 1000).}

\item{type}{Distribution type: \code{"left"} (default) or
\code{"right"}.}
}
\value{
A named list with elements:
  \describe{
    \item{p_value}{Bootstrap p-value.}
    \item{ks_statistic}{Observed KS statistic.}
    \item{alpha}{Alpha used for the test.}
    \item{x_min}{xMin used for the test.}
    \item{replicas}{Number of replicas used.}
  }
}
\description{
Computes a p-value for the hypothesis that \code{data} follow a discrete
power-law distribution using a semi-parametric bootstrap procedure
(Clauset, Shalizi & Newman 2009).  The bootstrap is parallelised with
\pkg{RcppParallel}.
}
\examples{
\dontrun{
set.seed(42)
data <- sample(1:500, 200, replace = TRUE, prob = (1:500)^(-2.5))
gof  <- powerlaw_gof(data, replicas = 500)
print(gof$p_value)
}

}
